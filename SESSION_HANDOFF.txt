SESSION HANDOFF

Project: ESP32-DevKitC3-Simple-Thermostat-PCB
Last Updated: 2026-04-30
Current Branch: main

What Changed This Session
- Added SHT45 sensor support in src/Main-Thermostat.cpp.
: Includes Adafruit_SHT4x, sensor enum value, instance, detect/init/read paths.
- Added SHT4x dependency in platformio.ini.
- Hardened WiFi/MQTT against blocking in src/Main-Thermostat.cpp.
: setupMQTT() sets espClient timeout to 3000ms to bound MQTT connect stalls.
: enterWiFiCredentials() exits after 120s to avoid indefinite UI/system lock.
- Added backup heat feature with relay conflict protection and emergency behavior refinements.
: Settings include backup enable, relay select (pump default / heat stage 2 / cool stage 2), delay minutes.
: Added failure-detection style backup logic and force-primary-off behavior when backup is active.
: Added extra tuning controls for backup in web UI: minimum temp rise and maximum temp drop thresholds.
- Updated OTA confirmation payload/UI support to include device date/time after reboot.
- Updated TFT main display interaction.
: Big center number now defaults to sensor temperature.
: Plus/minus first press switches center display to setpoint only (no setpoint change).
: Subsequent plus/minus presses adjust setpoint while in setpoint view.
: Center display auto-returns to sensor temperature after 15 seconds.
: Added "Set Temp" label below big center number during setpoint-edit mode.
: Label color is red for heat mode, blue for cool mode, orange for auto mode.
- Updated firmware organization scripts.
: Artifact filenames now include version and timestamp (bin/elf).
: Build folder name now includes timestamp and version: build_YYYYMMDD-HHMMSS_vVERSION.
: Retention default restored to keep 3 builds per variant.
- Reduced right-side sidebar text size from 2 to 1 (Set, humidity, pressure, air quality).
: Sidebar cursors shifted right to x=260 to keep items flush right on the 320px display.

Why
- Prevent lockups when WiFi is configured but unavailable, or when MQTT broker is unreachable.
- Keep display/controls responsive without requiring power cycle.
- Make backup heat behavior align with emergency/primary-failure expectations.
- Improve firmware artifact traceability and rollback handling.
- "Set Temp" label on big number makes it unambiguous what is being shown during edit mode.
- Smaller sidebar text frees horizontal space and avoids wrapping at screen edge.

Validation
- Build command used: pio run -e esp32-s3-wroom-1-n16
- Build command used: ./build.sh all
- Organizer command used: ./organize_firmware.sh
- Status at handoff: successful builds and firmware organization with versioned filenames/folders.
- Verified latest scripts reference versioned folders/files (example N16):
: firmware/latest_flash_N16.sh -> N16/build_20260430-171051_v1.4.016

Open Follow-ups
- Consider moving reconnectMQTT() into a dedicated task if longer broker outages still impact responsiveness.
- Consider replacing blocking delays in WiFi credential flow with non-blocking state machine.
- Consider adding MQTT entities for backup heat status and selected relay for Home Assistant visibility.
- Consider surfacing backup-heat threshold settings via MQTT/Home Assistant entities.
- Consider adding labels (e.g. "Hum:", "Press:") to sidebar items now that size-1 text leaves room.

Notes For Next Session
- If troubleshooting lockups, focus first on reconnectMQTT() and credential-entry loops.
- For TFT setpoint behavior issues, inspect handleButtonPress() and updateDisplay() in src/Main-Thermostat.cpp.
: displayShowingSetpoint / setpointDisplayUntil control center big-number mode.
: showSetTempNow drives both the big number and the "Set Temp" label color logic.
- Right-side sidebar uses setTextSize(1) at x=260, y=30/50/70/90 for Set/Hum/Pressure/AQ.
- Firmware outputs now live under firmware/[N8|N16|N32]/build_<timestamp>_v<version>.
